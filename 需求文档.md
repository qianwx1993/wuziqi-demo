# 五子棋网页游戏产品需求文档（PRD）

| 文档信息 | 内容 |
|---------|------|
| **文档名称** | 五子棋网页游戏产品需求文档 |
| **文档版本** | v1.0.0 |
| **创建日期** | 2026-04-21 |
| **项目代号** | Wuziqi-Web |
| **文档状态** | 初稿 |
| **编写人员** | AI Assistant |

---

## 文档修订历史

| 版本 | 日期 | 修订人 | 修订内容 |
|-----|------|--------|---------|
| v1.0.0 | 2026-04-21 | AI Assistant | 初始版本创建 |

---

## 目录

1. [项目概述](#1-项目概述)
2. [用户画像与使用场景](#2-用户画像与使用场景)
3. [功能需求规格说明](#3-功能需求规格说明)
4. [非功能性需求](#4-非功能性需求)
5. [系统架构设计](#5-系统架构设计)
6. [数据结构设计](#6-数据结构设计)
7. [接口设计](#7-接口设计)
8. [UI/UX设计规范](#8-uiux设计规范)
9. [状态机设计](#9-状态机设计)
10. [错误处理与边界条件](#10-错误处理与边界条件)
11. [测试用例](#11-测试用例)
12. [部署与发布](#12-部署与发布)
13. [未来扩展规划](#13-未来扩展规划)

---

## 1. 项目概述

### 1.1 项目背景

五子棋是一种经典的两人对弈棋类游戏，规则简单但策略性强。本项目旨在开发一个基于 Web 技术的五子棋游戏，为用户提供流畅、美观的对弈体验。

### 1.2 项目目标

| 目标维度 | 具体描述 |
|---------|---------|
| **业务目标** | 提供一个可即开即玩的五子棋游戏，支持双人对战模式 |
| **用户体验** | 界面简洁美观，操作流畅响应快，胜负判定准确 |
| **技术目标** | 使用原生 Web 技术实现，无需依赖第三方框架 |
| **性能目标** | 首次加载时间 < 1s，点击响应时间 < 50ms |

### 1.3 项目范围

**包含内容：**
- 15×15 标准五子棋棋盘
- 双人同屏对战模式
- 基础游戏控制（重新开始、悔棋）
- 胜负判定系统
- 游戏历史记录（用于悔棋）

**不包含内容：**
- 人机对战 AI
- 在线联机对战
- 用户账号系统
- 游戏回放功能
- 移动端原生应用

---

## 2. 用户画像与使用场景

### 2.1 目标用户

| 用户类型 | 描述 | 使用频率 | 核心需求 |
|---------|------|---------|---------|
| **休闲玩家** | 喜欢简单棋类游戏的普通用户，偶尔打发时间 | 低（每周1-2次） | 简单易用，无需登录 |
| **棋牌爱好者** | 对五子棋有一定了解，喜欢与朋友对弈 | 中（每周3-5次） | 操作流畅，体验真实 |
| **开发者** | 学习 Canvas 游戏开发的技术人员 | 一次（学习参考） | 代码规范，结构清晰 |

### 2.2 使用场景

**场景 A：朋友聚会**
- 场景描述：两位朋友在一起聊天，想玩个简单的游戏打发时间
- 使用流程：打开网页 → 开始对弈 → 一方获胜 → 重新开始

**场景 B：代码学习**
- 场景描述：前端开发者想学习 Canvas 游戏开发
- 使用流程：查看源码 → 学习架构 → 本地运行 → 修改实验

---

## 3. 功能需求规格说明

### 3.1 功能模块总览

```
┌─────────────────────────────────────────────────────────┐
│                      五子棋游戏系统                       │
├─────────────┬─────────────┬─────────────┬───────────────┤
│  棋盘显示   │  游戏控制   │  状态管理   │   胜负判定    │
├─────────────┼─────────────┼─────────────┼───────────────┤
│ • 网格绘制   │ • 落子操作   │ • 棋盘状态   │ • 四方向检测   │
│ • 棋子渲染   │ • 重新开始   │ • 历史记录   │ • 五子判断     │
│ • 标记显示   │ • 悔棋功能   │ • 当前玩家   │ • 胜负提示     │
└─────────────┴─────────────┴─────────────┴───────────────┘
```

### 3.2 核心功能详细说明

#### FR-01: 棋盘显示

| 功能项 | 详细说明 |
|-------|---------|
| **功能描述** | 在页面中绘制标准的 15×15 五子棋棋盘 |
| **输入参数** | 无 |
| **输出结果** | Canvas 渲染的棋盘界面 |
| **前置条件** | 页面加载完成 |
| **后置条件** | 棋盘可见，可接收点击事件 |
| **业务规则** | FR-01-1: 棋盘包含 225 个交叉点（15×15）<br>FR-01-2: 使用木纹色背景（#DEB887）<br>FR-01-3: 绘制 5 个星位点（中心点 + 四角星位）<br>FR-01-4: 网格线颜色为深棕色（#8B4513），线宽 1px |

#### FR-02: 落子操作

| 功能项 | 详细说明 |
|-------|---------|
| **功能描述** | 用户点击棋盘交叉点，在点击位置放置棋子 |
| **输入参数** | 鼠标点击坐标 (x, y) |
| **输出结果** | 在目标位置绘制棋子 |
| **前置条件** | 游戏进行中，点击位置为空 |
| **后置条件** | 棋盘状态更新，当前玩家切换 |
| **业务规则** | FR-02-1: 黑棋先行，双方轮流落子<br>FR-02-2: 点击位置距离交叉点 20px 内才有效<br>FR-02-3: 已有棋子的位置不可重复落子<br>FR-02-4: 每次落子后标记最后落子位置 |

#### FR-03: 胜负判定

| 功能项 | 详细说明 |
|-------|---------|
| **功能描述** | 检测当前落子是否形成五子连珠 |
| **输入参数** | 最后落子位置 (row, col) |
| **输出结果** | 布尔值（是否获胜）/ 获胜方 |
| **前置条件** | 至少落子 5 次 |
| **后置条件** | 判定获胜则结束游戏 |
| **业务规则** | FR-03-1: 检测水平、垂直、主对角线、副对角线四个方向<br>FR-03-2: 连续 5 子或以上即判定获胜<br>FR-03-3: 获胜后显示弹窗提示，禁止继续落子 |

#### FR-04: 重新开始

| 功能项 | 详细说明 |
|-------|---------|
| **功能描述** | 重置游戏状态，开始新对局 |
| **输入参数** | 无 |
| **输出结果** | 棋盘清空，状态重置 |
| **前置条件** | 无 |
| **后置条件** | 游戏回到初始状态 |
| **业务规则** | FR-04-1: 清空所有棋子<br>FR-04-2: 重置当前玩家为黑棋<br>FR-04-3: 清空历史记录<br>FR-04-4: 不显示确认对话框（直接执行） |

#### FR-05: 悔棋

| 功能项 | 详细说明 |
|-------|---------|
| **功能描述** | 撤销最后一次落子，恢复到上一步状态 |
| **输入参数** | 无 |
| **输出结果** | 最后一步棋被移除 |
| **前置条件** | 历史记录不为空 |
| **后置条件** | 棋盘状态回退，当前玩家切换 |
| **业务规则** | FR-05-1: 每次悔棋撤销一步<br>FR-05-2: 悔棋后切换回原执棋方<br>FR-05-3: 至少已落子 1 步才能悔棋<br>FR-05-4: 悔棋后更新最后落子标记 |

#### FR-06: 状态显示

| 功能项 | 详细说明 |
|-------|---------|
| **功能描述** | 实时显示当前游戏状态信息 |
| **输入参数** | 当前玩家、游戏状态 |
| **输出结果** | UI 状态文本更新 |
| **前置条件** | 无 |
| **后置条件** | 用户可见当前状态 |
| **业务规则** | FR-06-1: 显示当前执棋方（黑棋/白棋）<br>FR-06-2: 使用棋子符号标识（● 黑 / ○ 白）<br>FR-06-3: 获胜后显示获胜方信息 |

---

## 4. 非功能性需求

### 4.1 性能需求

| 指标 | 目标值 | 测量方法 |
|-----|--------|---------|
| **首次加载时间** | ≤ 1 秒 | 页面 load 事件触发时间 |
| **点击响应时间** | ≤ 50 毫秒 | 从 click 事件到棋子绘制完成 |
| **渲染帧率** | 60 FPS | 棋盘绘制动画流畅度 |
| **内存占用** | ≤ 10 MB | 浏览器任务管理器 |

### 4.2 兼容性需求

| 浏览器 | 最低版本 | 说明 |
|-------|---------|------|
| **Chrome** | 90+ | 推荐使用 |
| **Firefox** | 88+ | 完全支持 |
| **Safari** | 14+ | 完全支持 |
| **Edge** | 90+ | 完全支持 |

### 4.3 可访问性需求

- 支持键盘操作（可选）
- 颜色对比度符合 WCAG 2.1 AA 标准
- 支持屏幕阅读器（基础语义化标签）

### 4.4 代码质量需求

| 指标 | 目标值 |
|-----|--------|
| **代码覆盖率** | ≥ 80%（核心逻辑） |
| **ESLint 规范** | 0 errors, 0 warnings |
| **JSDoc 覆盖** | 100%（公共 API） |
| **圈复杂度** | ≤ 10（每个函数） |

---

## 5. 系统架构设计

### 5.1 架构图

```
┌─────────────────────────────────────────────────────────┐
│                      Presentation Layer                 │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────┐  │
│  │ index.html │  │ main.css   │  │    UI Controls   │  │
│  └────────────┘  └────────────┘  └──────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                      Application Layer                  │
│  ┌──────────────────────────────────────────────────┐  │
│  │                   Game Class                     │  │
│  │  • 状态管理  • 流程控制  • 协调各模块             │  │
│  └────────────┬─────────────────────────────────────┘  │
│               │                                         │
│  ┌────────────▼────────────┐  ┌─────────────────────┐  │
│  │      Board Class        │  │   RuleEngine Class  │  │
│  │  • Canvas 渲染          │  │  • 胜负判定逻辑     │  │
│  │  • 事件处理             │  │  • 规则验证         │  │
│  │  • 坐标转换             │  │                     │  │
│  └─────────────────────────┘  └─────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                       Data Layer                        │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────┐  │
│  │   Config   │  │   History  │  │   GameState      │  │
│  │  (常量配置) │  │  (栈结构)  │  │   (棋盘数组)     │  │
│  └────────────┘  └────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 5.2 模块职责

| 模块 | 职责 | 依赖 |
|-----|------|-----|
| **Game** | 游戏主控制器，管理游戏流程 | Board, RuleEngine, Config |
| **Board** | 棋盘渲染与交互处理 | Config, GameState |
| **RuleEngine** | 胜负判定规则引擎 | GameState |
| **Config** | 配置常量管理 | 无 |
| **History** | 历史记录管理（悔棋用） | 无 |

---

## 6. 数据结构设计

### 6.1 棋盘状态（GameState）

```javascript
/**
 * 棋盘状态数据结构
 * 二维数组：15×15
 * 0 = 空位, 1 = 黑棋, 2 = 白棋
 */
const gameState = [
    [0, 0, 0, ..., 0],  // 第 0 行
    [0, 0, 0, ..., 0],  // 第 1 行
    // ... 共 15 行
    [0, 0, 0, ..., 0],  // 第 14 行
];
```

### 6.2 历史记录（History）

```javascript
/**
 * 历史记录数据结构
 * 栈结构，存储每一步的落子信息
 */
const history = [
    { row: 7, col: 7, player: 1, timestamp: 1234567890 },
    { row: 7, col: 8, player: 2, timestamp: 1234567891 },
    // ... 更多记录
];
```

### 6.3 配置常量（Config）

```javascript
/**
 * 游戏配置常量
 */
const CONFIG = {
    // 棋盘配置
    BOARD_SIZE: 15,           // 棋盘行列数
    CELL_SIZE: 40,            // 格子像素大小
    BOARD_PADDING: 30,        // 棋盘边距

    // 交互配置
    CLICK_TOLERANCE: 20,      // 点击容差（像素）

    // 颜色配置
    COLORS: {
        BACKGROUND: '#DEB887',      // 木纹色
        GRID_LINE: '#8B4513',       // 网格线
        BLACK_PIECE: '#000000',     // 黑棋
        WHITE_PIECE: '#FFFFFF',     // 白棋
        LAST_MOVE: '#FF0000',       // 最后落子标记
        STAR_POINT: '#8B4513',      // 星位点
    },

    // 玩家枚举
    PLAYER: {
        EMPTY: 0,
        BLACK: 1,
        WHITE: 2,
    },
};
```

---

## 7. 接口设计

### 7.1 Board 类接口

```javascript
/**
 * 棋盘类：负责棋盘渲染和交互
 */
class Board {
    /**
     * 构造函数
     * @param {HTMLCanvasElement} canvas - Canvas 元素
     * @param {Object} config - 配置对象
     */
    constructor(canvas, config);

    /**
     * 绘制棋盘
     */
    drawBoard();

    /**
     * 绘制棋子
     * @param {number} row - 行索引
     * @param {number} col - 列索引
     * @param {number} player - 玩家（1=黑, 2=白）
     */
    drawPiece(row, col, player);

    /**
     * 绘制最后落子标记
     * @param {number} row - 行索引
     * @param {number} col - 列索引
     */
    drawLastMoveMarker(row, col);

    /**
     * 清除标记
     */
    clearMarkers();

    /**
     * 坐标转换：屏幕坐标 → 棋盘坐标
     * @param {number} x - 屏幕 x 坐标
     * @param {number} y - 屏幕 y 坐标
     * @returns {{row: number, col: number}|null} 棋盘坐标，无效返回 null
     */
    screenToBoard(x, y);

    /**
     * 棋盘坐标 → 屏幕坐标
     * @param {number} row - 行索引
     * @param {number} col - 列索引
     * @returns {{x: number, y: number}} 屏幕坐标
     */
    boardToScreen(row, col);

    /**
     * 清空棋盘
     */
    clear();

    /**
     * 设置点击事件处理器
     * @param {Function} handler - 点击处理函数
     */
    setClickHandler(handler);
}
```

### 7.2 RuleEngine 类接口

```javascript
/**
 * 规则引擎类：负责胜负判定
 */
class RuleEngine {
    /**
     * 构造函数
     * @param {Object} config - 配置对象
     */
    constructor(config);

    /**
     * 检查落子是否有效
     * @param {Array<Array<number>>} board - 棋盘状态
     * @param {number} row - 行索引
     * @param {number} col - 列索引
     * @returns {boolean} 是否有效
     */
    isValidMove(board, row, col);

    /**
     * 检查是否获胜
     * @param {Array<Array<number>>} board - 棋盘状态
     * @param {number} row - 最后落子行索引
     * @param {number} col - 最后落子列索引
     * @returns {boolean} 是否获胜
     */
    checkWin(board, row, col);

    /**
     * 检查是否平局（棋盘已满）
     * @param {Array<Array<number>>} board - 棋盘状态
     * @returns {boolean} 是否平局
     */
    checkDraw(board);

    /**
     * 获取落子建议（预留接口）
     * @param {Array<Array<number>>} board - 棋盘状态
     * @param {number} player - 玩家
     * @returns {{row: number, col: number}} 建议落子位置
     */
    getSuggestion(board, player);
}
```

### 7.3 Game 类接口

```javascript
/**
 * 游戏主控制类
 */
class Game {
    /**
     * 构造函数
     * @param {Board} board - 棋盘实例
     * @param {RuleEngine} ruleEngine - 规则引擎实例
     * @param {Object} config - 配置对象
     */
    constructor(board, ruleEngine, config);

    /**
     * 开始新游戏
     */
    startNewGame();

    /**
     * 落子
     * @param {number} row - 行索引
     * @param {number} col - 列索引
     * @returns {boolean} 是否成功
     */
    makeMove(row, col);

    /**
     * 悔棋
     * @returns {boolean} 是否成功
     */
    undoMove();

    /**
     * 获取当前玩家
     * @returns {number} 玩家（1=黑, 2=白）
     */
    getCurrentPlayer();

    /**
     * 获取游戏状态
     * @returns {string} 状态（playing/finished）
     */
    getGameState();

    /**
     * 获取获胜方
     * @returns {number|null} 获胜玩家，无则返回 null
     */
    getWinner();

    /**
     * 注册状态变化监听器
     * @param {Function} callback - 回调函数
     */
    onStateChange(callback);

    /**
     * 注册游戏结束监听器
     * @param {Function} callback - 回调函数
     */
    onGameOver(callback);
}
```

---

## 8. UI/UX设计规范

### 8.1 视觉设计

#### 8.1.1 色彩规范

| 用途 | 颜色值 | 说明 |
|-----|--------|------|
| **页面背景** | linear-gradient(135deg, #667eea 0%, #764ba2 100%) | 渐变紫色 |
| **棋盘背景** | #DEB887 | 木纹色 |
| **网格线** | #8B4513 | 深棕色 |
| **黑棋** | radial-gradient(#555 → #000) | 渐变黑 |
| **白棋** | radial-gradient(#fff → #ddd) | 渐变白 |
| **最后落子标记** | #FF0000 | 红色小点 |
| **主按钮** | #667eea | 紫色 |
| **按钮悬停** | #764ba2 | 深紫色 |

#### 8.1.2 尺寸规范

| 元素 | 尺寸 | 说明 |
|-----|------|------|
| **棋盘** | 660×660 px | 15格×40px + 30px边距×2 |
| **棋子半径** | 17 px | CELL_SIZE/2 - 3 |
| **星位点半径** | 5 px | - |
| **最后落子标记半径** | 4 px | - |
| **按钮高度** | 40 px | - |
| **按钮圆角** | 5 px | - |

### 8.2 交互设计

#### 8.2.1 交互状态

| 元素 | 正常状态 | 悬停状态 | 点击状态 | 禁用状态 |
|-----|---------|---------|---------|---------|
| **按钮** | 默认色 | 背景加深 | 轻微缩小 | 灰色半透明 |
| **棋盘** | 默认 | 鼠标指针变为十字 | - | - |
| **交叉点** | - | 显示半透明预览棋子 | - | - |

#### 8.2.2 动画效果

| 交互 | 动画效果 | 持续时间 | 缓动函数 |
|-----|---------|---------|---------|
| **落子** | 棋子从 0 缩放到 1 | 150ms | ease-out |
| **按钮悬停** | 阴影加深 + 向上 2px | 200ms | ease-in-out |
| **胜负弹窗** | 淡入 + 缩放 | 300ms | ease-out |

### 8.3 响应式设计

| 屏幕尺寸 | 棋盘大小 | 布局调整 |
|---------|---------|---------|
| **≥ 1200px** | 660×660 | 居中显示 |
| **768px - 1199px** | 550×550 | 缩小棋盘 |
| **< 768px** | 适配屏幕宽度 | 纵向布局 |

---

## 9. 状态机设计

### 9.1 游戏状态机

```
                    ┌──────────────┐
                    │   IDLE       │  初始状态
                    └──────┬───────┘
                           │ startNewGame()
                           ▼
                    ┌──────────────┐
        ┌──────────►│  PLAYING     │◄─────────┐
        │           └──────┬───────┘          │
        │                  │ makeMove()       │ undoMove()
        │                  ▼                  │
        │           ┌──────────────┐          │
        │           │  CHECKING    │          │
        │           └──────┬───────┘          │
        │                  │                  │
        │           ┌──────┴──────┐           │
        │           │             │           │
        │      checkWin()    checkDraw()      │
        │           │             │           │
        │           ▼             ▼           │
        │    ┌──────────┐  ┌──────────┐      │
        │    │ FINISHED │  │  DRAW    │      │
        │    └──────────┘  └──────────┘      │
        │                                       │
        └───────────────────────────────────────┘
                    startNewGame()
```

### 9.2 状态定义

| 状态 | 描述 | 可执行操作 |
|-----|------|-----------|
| **IDLE** | 游戏未开始 | startNewGame() |
| **PLAYING** | 游戏进行中 | makeMove(), undoMove(), startNewGame() |
| **CHECKING** | 正在检查胜负 | 无（内部状态） |
| **FINISHED** | 游戏结束（有胜者） | startNewGame() |
| **DRAW** | 游戏平局 | startNewGame() |

---

## 10. 错误处理与边界条件

### 10.1 异常情况处理

| 异常情况 | 处理方式 | 用户提示 |
|---------|---------|---------|
| **点击棋盘外** | 忽略点击 | 无提示 |
| **点击已有棋子位置** | 忽略点击 | 可选：短暂抖动动画 |
| **点击距离交叉点过远** | 忽略点击 | 无提示 |
| **游戏结束后落子** | 忽略操作 | 弹窗提示"游戏已结束" |
| **无历史记录时悔棋** | 忽略操作 | 按钮置灰或禁用 |

### 10.2 边界条件测试

| 场景 | 预期行为 |
|-----|---------|
| **第一步落子** | 黑棋落子，无历史记录 |
| **棋盘第一步（0,0）** | 正常落子 |
| **棋盘最后一步（14,14）** | 正常落子 |
| **连续悔棋到空棋盘** | 悔棋按钮禁用 |
| **第一步获胜（不可能）** | 无需处理（至少需5步） |
| **棋盘填满仍未获胜** | 判定为平局 |

---

## 11. 测试用例

### 11.1 单元测试

| 测试项 | 测试用例 | 预期结果 |
|-------|---------|---------|
| **坐标转换** | screenToBoard(50, 50) | {row: 0, col: 0} |
| **坐标转换** | screenToBoard(700, 700) | null |
| **胜负判定-横** | 连续5子横线 | 返回 true |
| **胜负判定-竖** | 连续5子竖线 | 返回 true |
| **胜负判定-斜** | 连续5子斜线 | 返回 true |
| **胜负判定-未完成** | 连续4子 | 返回 false |

### 11.2 集成测试

| 测试场景 | 操作步骤 | 预期结果 |
|---------|---------|---------|
| **完整对局** | 落子至一方获胜 | 正确判定胜负 |
| **悔棋流程** | 落子 → 悔棋 | 棋子消失，状态回退 |
| **重新开始** | 落子后点击重新开始 | 棋盘清空，黑棋先手 |
| **边界落子** | 在四角落子 | 正常落子 |

### 11.3 UI测试

| 测试项 | 检查点 | 通过标准 |
|-------|--------|---------|
| **棋盘显示** | 网格线、星位点 | 清晰可见 |
| **棋子显示** | 黑白棋颜色分明 | 无视觉混淆 |
| **按钮交互** | 悬停、点击效果 | 响应正常 |
| **响应式** | 不同屏幕尺寸 | 布局自适应 |

---

## 12. 部署与发布

### 12.1 部署要求

| 项目 | 要求 |
|-----|------|
| **Web服务器** | 任意静态文件服务器（Nginx/Apache/GitHub Pages） |
| **HTTPS** | 推荐启用（非强制） |
| **域名** | 任意 |
| **CDN** | 无需（纯静态资源） |

### 12.2 文件清单

```
dist/
├── index.html          # 主页面（~2 KB）
├── assets/
│   ├── css/
│   │   └── main.css   # 样式文件（~5 KB）
│   └── js/
│       ├── config.js  # 配置模块（~1 KB）
│       ├── board.js   # 棋盘模块（~8 KB）
│       ├── rules.js   # 规则模块（~5 KB）
│       ├── game.js    # 游戏控制（~6 KB）
│       └── main.js    # 入口文件（~1 KB）
└── README.md           # 说明文档
```

**总大小估算：约 30 KB（未压缩）**

---

## 13. 未来扩展规划

### 13.1 Phase 2 功能

| 功能 | 优先级 | 说明 |
|-----|--------|------|
| **人机对战** | 高 | 基于评分函数的 AI 对手 |
| **音效** | 中 | 落子音、获胜音效 |
| **落子提示** | 中 | 显示当前鼠标位置的半透明棋子 |
| **游戏计时** | 低 | 双方限时模式 |

### 13.2 Phase 3 功能

| 功能 | 优先级 | 说明 |
|-----|--------|------|
| **在线联机** | 高 | WebSocket 实时对战 |
| **用户系统** | 中 | 账号、积分、排行榜 |
| **游戏回放** | 低 | 对局记录回放 |
| **移动端 App** | 低 | PWA 或原生应用 |

---

## 附录

### A. 参考资料

- 五子棋规则：[Wikipedia - Gomoku](https://en.wikipedia.org/wiki/Gomoku)
- Canvas API：[MDN - Canvas API](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API)
- ES6 模块：[MDN - ES6 Modules](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Modules)

### B. 术语表

| 术语 | 定义 |
|-----|------|
| **天元** | 棋盘正中心点（7,7） |
| **星位** | 棋盘上的标记点，用于辅助定位 |
| **连珠** | 连续多个同色棋子 |
| **先手** | 第一位落子的玩家（黑棋） |
| **后手** | 后落子的玩家（白棋） |

### C. 变更记录

本文档如有变更，将在"文档修订历史"中记录。

---

**文档结束**
